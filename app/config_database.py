def config():
    """This sets up the parameters for connecting to the database.
        The host, db, pasword and user
    """
    db = {
        'host': 'localhost',
        'database': 'mydiary',
        'user': 'koin',
        'password': 'kiki'
    }

    return db